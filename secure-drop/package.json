{
  "name": "redact-secure-drop",
  "version": "1.0.0",
  "description": "Secure Drop Server for Redact — receives encrypted ciphertext bundles from field agents",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "uuid": "^9.0.0"
  }
}
